/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Note: In Next.js 13+, API route-specific config is handled in the route file itself
  // using export const config = { api: { bodyParser: false } }
}

module.exports = nextConfig
